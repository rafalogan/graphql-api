type User {
    id: Int
    name: String!
    email: String!
    profiles: [Perfil]
}

input UserInput {
    name: String
    email: String
    password: String
    profiles: [PerfilFilter]
}

input UserFilter {
    id: Int
    email: String
}
